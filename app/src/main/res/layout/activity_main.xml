<?xml version="1.0" encoding="utf-8"?>
<!-- Map3DView fills the screen. ARCore needs a real, continuously rendering
     GL surface to stay in TRACKING; it is kept off-screen (same size, negative
     margins) so nothing visible covers the map. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.blindvision.arpose.gl.Map3DView
        android:id="@+id/map_3d"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.opengl.GLSurfaceView
        android:id="@+id/gl_surface"
        android:layout_width="120dp"
        android:layout_height="160dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="-180dp"
        android:layout_marginEnd="-132dp" />

    <!-- Prompt overlay: dimmed scrim + card until the user picks a destination -->
    <FrameLayout
        android:id="@+id/destination_prompt_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:focusable="false"
        android:visibility="gone">

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#66000000" />

        <TextView
            android:id="@+id/destination_prompt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/prompt_card_bg"
            android:gravity="center"
            android:paddingHorizontal="28dp"
            android:paddingVertical="22dp"
            android:text="@string/destination_prompt"
            android:textColor="#FFFFFFFF"
            android:textSize="20sp"
            android:fontFamily="sans-serif-medium"
            android:lineSpacingMultiplier="1.35" />
    </FrameLayout>

    <LinearLayout
        android:id="@+id/search_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="vertical"
        android:paddingHorizontal="12dp"
        android:paddingTop="12dp"
        android:paddingBottom="8dp"
        android:background="#E6FFFFFF"
        android:elevation="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <EditText
                android:id="@+id/search_input"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:background="@android:drawable/editbox_background_normal"
                android:hint="@string/search_hint"
                android:imeOptions="actionSearch"
                android:inputType="text"
                android:maxLines="1"
                android:paddingHorizontal="12dp"
                android:textColor="#111111"
                android:textColorHint="#888888"
                android:textSize="16sp" />

            <ImageButton
                android:id="@+id/mic_button"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/recenter_button_bg"
                android:contentDescription="@string/mic_button_desc"
                android:padding="12dp"
                android:scaleType="centerInside"
                android:src="@android:drawable/ic_btn_speak_now" />

            <ImageButton
                android:id="@+id/search_button"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/recenter_button_bg"
                android:contentDescription="@string/search_button_desc"
                android:padding="12dp"
                android:scaleType="centerInside"
                android:src="@android:drawable/ic_menu_search" />
        </LinearLayout>

        <TextView
            android:id="@+id/destination_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:paddingHorizontal="4dp"
            android:textColor="#1565C0"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium"
            android:visibility="gone" />

        <TextView
            android:id="@+id/search_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:paddingHorizontal="4dp"
            android:textColor="#C62828"
            android:textSize="13sp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Recenter button: bottom-right floating button -->
    <ImageButton
        android:id="@+id/recenter_button"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="24dp"
        android:layout_marginEnd="16dp"
        android:background="@drawable/recenter_button_bg"
        android:contentDescription="@string/recenter_desc"
        android:padding="12dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_my_location"
        android:elevation="6dp" />

    <TextView
        android:id="@+id/loading_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="#CC101418"
        android:padding="16dp"
        android:text="Loading map…"
        android:textColor="#FFFFFFFF"
        android:textSize="15sp"
        android:fontFamily="sans-serif-medium"
        android:visibility="visible" />
</FrameLayout>
