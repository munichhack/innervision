*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
app/build/
.externalNativeBuild/
.cxx/

# Project-local, self-contained toolchain (JDK + Android SDK). Safe to delete.
/.toolchain/
# Large input data (floor-plan masks / generated CSVs)
/data/

# Secrets (API keys for the destination resolver)
.env
